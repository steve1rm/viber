<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- hide the 'left' and 'top' so only the buttom and right is displayed -->
    <item android:top="-3dp" android:left="-3dp" android:right="-3dp">
        <shape>
            <solid android:color="@color/colorDialerPressed"/>
            <stroke android:width="2dp" android:color="@color/colorBorders"/>
        </shape>
    </item>

    <item android:top="12dp" android:bottom="24dp" android:right="24dp" android:left="24dp">
        <bitmap android:src="@drawable/ic_volume_up_black_48dp"/>
    </item>
</layer-list>