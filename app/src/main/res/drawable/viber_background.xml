<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- bottom layer in the layer list -->
    <item android:drawable="@drawable/background_viber"/>

    <!-- Top layer in the layer list i.e display over the background -->
    <item
        android:drawable="@drawable/inner_shape"
        android:top="20dp"
        android:bottom="20dp"
        android:left="20dp"
        android:right="20dp"/>
</layer-list>